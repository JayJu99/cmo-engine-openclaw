# H2 Scripts Notes

This directory records non-runtime notes for H2 validation scaffolding.

The optional script `scripts/validate-hermes-cmo-h2.mjs` only parses H1/H2 JSON examples and schemas. It does not call external APIs, start agents, write Vault, mutate Supabase, or modify runtime state.

